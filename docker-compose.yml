services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: yplaza
      POSTGRES_USER: yplaza
      POSTGRES_PASSWORD: yplaza
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
